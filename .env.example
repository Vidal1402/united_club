# ========== DATABASE (obrigatório) ==========
# MongoDB Atlas: Dashboard do cluster → Connect → Drivers → copie a connection string.
# Inclua o nome do banco na URL (antes do ?). Senha com #, = ou @: use %23, %3D, %40.
DATABASE_URL="mongodb+srv://USUARIO:SENHA@cluster.xxxxx.mongodb.net/NOME_DO_BANCO?retryWrites=true&w=majority"

# ========== JWT (obrigatório) ==========
# Gere chaves fortes (mín. 32 caracteres). Ex.: openssl rand -base64 32
JWT_SECRET=sua-chave-secreta-access-token-minimo-32-caracteres
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=outra-chave-secreta-refresh-token-minimo-32-chars
JWT_REFRESH_EXPIRES_IN=7d

# ========== APP ==========
PORT=3000
NODE_ENV=development

# Origens permitidas para CORS (frontend). Várias: separadas por vírgula, sem espaço.
# Ex.: http://localhost:5173 ou https://meu-app.vercel.app
CORS_ORIGIN=http://localhost:5173

# ========== S3 COMPATÍVEL (opcional – avatares, uploads) ==========
# Só preencha se for usar upload de arquivos. Serviços compatíveis: AWS S3, MinIO, Cloudflare R2, DigitalOcean Spaces, Backblaze B2.

# --- AWS S3 ---
# S3_ENDPOINT=  (deixe vazio para AWS padrão)
# S3_ACCESS_KEY=AKIA...
# S3_SECRET_KEY=...
# S3_BUCKET=united-club-uploads
# S3_REGION=us-east-1

# --- MinIO (local ou self-hosted) ---
# S3_ENDPOINT=http://localhost:9000
# S3_ACCESS_KEY=minioadmin
# S3_SECRET_KEY=minioadmin
# S3_BUCKET=united-club-uploads
# S3_REGION=us-east-1

# --- Cloudflare R2 ---
# S3_ENDPOINT=https://SEU_ACCOUNT_ID.r2.cloudflarestorage.com
# S3_ACCESS_KEY= (R2 API Token – Access Key)
# S3_SECRET_KEY= (R2 API Token – Secret Key)
# S3_BUCKET=united-club-uploads
# S3_REGION=auto

# --- DigitalOcean Spaces ---
# S3_ENDPOINT=https://nyc3.digitaloceanspaces.com
# S3_ACCESS_KEY= (Spaces key)
# S3_SECRET_KEY= (Spaces secret)
# S3_BUCKET=united-club-uploads
# S3_REGION=nyc3

# Se NÃO for usar upload por enquanto, deixe vazio (o app funciona sem S3):
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=united-club-uploads
S3_REGION=us-east-1

# ========== RATE LIMIT (opcional) ==========
THROTTLE_TTL=60
THROTTLE_LIMIT=100
